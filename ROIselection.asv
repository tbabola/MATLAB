%load file
cd 'M:\Bergles Lab Data\Projects\In vivo imaging';
[fn path] = uigetfile();

movie = loadTif([path fn],8);
rotateCCW = imrotate(movie,55);
CCW = figure;
imagesc(rotateCCW(:,:,50));
RICpos = imrect(gca,[0,0,125,100]);
setRisizable(RICpos,0);
wait(RICpos);

rotateCW = imrotate(movie,-55);

