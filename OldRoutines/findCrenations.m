function [locs,pks] findCrenationPks(signal)
    time = zeros(1,size(signal(:),1));
    time = (1:1:size(signal(:),1));
    time = reshape(time,[size(signal,1),size(signal,2)]);
    
    
    
   
end