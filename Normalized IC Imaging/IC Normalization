%% load image
[fname, dname] = uigetfile('*.tif','Multiselect','on');
X = loadTif([dname fname],16);
[m,n,t] = size(X);
Xreshape = reshape(X,m*n,t)';

%% get 25th percentiles

%%get Fo and display it

%% Fav - Fo ./Fo

%get average intensity
